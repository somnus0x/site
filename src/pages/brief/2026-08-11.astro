---
import Layout from '../../layouts/Layout.astro';

// Auto-generated by generate-dashboard.py — 2026-08-11T06:55:01.767094+07:00
// Do not edit manually

const date = "Tuesday, August 11, 2026";
const updateTime = "06:55 BKK";
const volNum = 223;
const headline = "Bitcoin Dips to $63,913";
const subheadline = "real rates restrictive at 1.34% \u00b7 Fear & Greed at 30";

const fng = {"value": "30", "label": "Fear"};

const ticker = [{"sym": "BTC", "pr": "$63,913", "chg": -1.49, "label": null}, {"sym": "ETH", "pr": "$1,871", "chg": -2.07, "label": null}, {"sym": "SOL", "pr": "$75.98", "chg": -0.44, "label": null}, {"sym": "F&G", "pr": "30", "chg": null, "label": "Fear"}];
const signals = [{"coin": "BTC", "signal": "neutral", "confidence": 37, "rsi1h": 32.69, "rsi4h": 35.85, "change24h": -1.45, "reasons": []}, {"coin": "ETH", "signal": "neutral", "confidence": 57, "rsi1h": 29.35, "rsi4h": 34.37, "change24h": -2.0, "reasons": ["1h RSI oversold (29.35)"]}, {"coin": "SOL", "signal": "neutral", "confidence": 40, "rsi1h": 43.45, "rsi4h": 53.95, "change24h": -0.33, "reasons": []}, {"coin": "ATOM", "signal": "neutral", "confidence": 63, "rsi1h": 55.93, "rsi4h": 60.69, "change24h": 1.96, "reasons": []}, {"coin": "INIT", "signal": "neutral", "confidence": 40, "rsi1h": 36.42, "rsi4h": 49.33, "change24h": -2.23, "reasons": []}];
const positioning = [{"coin": "BTC", "retailLong": 61.5, "whaleLong": 62.5, "oi": "$6.8B", "oiTrend": "flat", "divergence": null, "retailTrend": "shifting long \u2014 crowd getting greedy"}, {"coin": "ETH", "retailLong": 72.2, "whaleLong": 57.6, "oi": "$4.4B", "oiTrend": "flat", "divergence": "notable divergence (retail: 2.60, whales: 1.36)", "retailTrend": "stable"}, {"coin": "SOL", "retailLong": 66.8, "whaleLong": 62.3, "oi": "$646.3M", "oiTrend": "flat", "divergence": "notable divergence (retail: 2.02, whales: 1.66)", "retailTrend": "stable"}];

const fred = [{"label": "Fed Funds Rate", "value": "3.63%", "change": 0.0}, {"label": "10Y-2Y Spread", "value": "0.47%", "change": 0.01}, {"label": "Breakeven Inflation", "value": "2.29%", "change": 0.04}, {"label": "Unemployment", "value": "4.1%", "change": -0.1}, {"label": "Trade-Weighted USD", "value": "119.0649", "change": -0.45}, {"label": "HY Spread", "value": "2.7%", "change": -0.01}];
const cycle = {"position": "Post-Inversion Watch", "realRate": "1.34%", "realRateDir": "restrictive", "curve": "Post-Inversion Normalization", "hy": "2.7%", "unemployment": "4.1%", "fedFunds": "3.63%", "breakeven": "2.29%"};

const macro = {"totalMC": "$2.3T", "btcDom": 56.5, "ethDom": 9.9, "TNX": 0, "GLD": 0};

const narrative = "Sentiment neutral at 30. Tape waiting for a catalyst. Real rate at +1.34% (restrictive). Yield curve post-inversion normalization. HY spread at 2.7% \u2014 credit markets pricing complacency. Positioning divergence: ETH: retail 72% long vs whales 58% long. SOL: retail 67% long vs whales 62% long.";

const polymarket = [{"q": "Will the Fed increase interest rates by 25 bps after the September 2026 meeting?", "yes": 43, "vol": "$939,878"}, {"q": "Will there be no change in Fed interest rates after the September 2026 meeting?", "yes": 55, "vol": "$579,575"}, {"q": "Will Kai and Speed beat the Minecraft challenge by August 17?", "yes": 11, "vol": "$454,085"}, {"q": "Clarity Act (H.R.3633) signed into law in 2026?", "yes": 24, "vol": "$431,758"}];
const oddMarkets = [{"q": "Will Trump say \"Bitcoin\", \"Crypto\", or \"Cryptocurrency\" during remarks in Las Ve", "yes": 4, "vol": "$34,659", "exchange": "polymarket"}, {"q": "Will Trump post \"Bitcoin,\" \"Crypto,\" or \"Cryptocurrency\" on Truth Social this we", "yes": 6, "vol": "$1,096", "exchange": "polymarket"}, {"q": "Will Elon post \"Bitcoin,\" \"Crypto,\" or \"Cryptocurrency\" on X this week?", "yes": 14, "vol": "$300.00", "exchange": "polymarket"}];
const trendingPools = [{"name": "TROLL / WETH 1%", "network": "robinhood", "vol": "$811,785", "change": -29.62}, {"name": "NYANCAT / WETH 1%", "network": "robinhood", "vol": "$174,860", "change": -26.16}, {"name": "PEPECOIN / WETH 1%", "network": "robinhood", "vol": "$160,907", "change": -61.28}, {"name": "CASHCAT / WETH 1%", "network": "robinhood", "vol": "$4.9M", "change": 17.82}, {"name": "TOSHI / WETH 0.25%", "network": "robinhood", "vol": "$205,497", "change": 220.4}];
const tvl = [{"name": "Binance CEX", "tvl": "$138.5B", "change": -1.27}, {"name": "OKX", "tvl": "$21.1B", "change": -1.21}, {"name": "Lido", "tvl": "$17.7B", "change": -2.61}, {"name": "Bitfinex", "tvl": "$16.8B", "change": -1.05}, {"name": "Aave V3", "tvl": "$14.0B", "change": -1.67}, {"name": "Bybit", "tvl": "$13.1B", "change": -0.43}, {"name": "Robinhood", "tvl": "$11.6B", "change": -2.1}, {"name": "SSV Network", "tvl": "$9.2B", "change": -1.53}];
const emerging = [{"name": "Bridge", "score": 162.8, "tvl": "$44.9B", "mom": 3099396.6, "breadth": 60, "whale": 16, "new90": 3, "protocols": 73, "leader": "WBTC", "delta7": 137.7}, {"name": "Dexs", "score": 67.1, "tvl": "$11.2B", "mom": 49.4, "breadth": 54, "whale": 16, "new90": 10, "protocols": 236, "leader": "PancakeSwap AMM", "delta7": 40.1}, {"name": "Token Locker", "score": 62.9, "tvl": "$236.5M", "mom": 6.7, "breadth": 86, "whale": 46, "new90": 1, "protocols": 8, "leader": "UNCX Network V2", "delta7": 28.2}, {"name": "Yield", "score": 52.7, "tvl": "$4.4B", "mom": 7.0, "breadth": 60, "whale": 27, "new90": 6, "protocols": 83, "leader": "Pendle", "delta7": 20.2}, {"name": "RWA Lending", "score": 52.5, "tvl": "$946.3M", "mom": 3.1, "breadth": 67, "whale": 27, "new90": 1, "protocols": 9, "leader": "Aave Horizon RWA", "delta7": -29.2}, {"name": "Cross Chain Bridge", "score": 50.2, "tvl": "$277.7M", "mom": 3.2, "breadth": 50, "whale": 33, "new90": 1, "protocols": 10, "leader": "Hyperlane", "delta7": 17.1}];
const emergingMeta = {"scanned": 8009, "scored": 30, "days": 13};
const discovery = [{"handle": "@xStocksFi", "sector": "RWA", "claim": "xStocks goes live on Hyperliquid HyperCore with 5 tokenized equities/ETFs for 24/7 spot liquidity.", "eng": "1.2K", "time": "Aug 10 17:20", "url": "https://x.com/xStocksFi/status/2086865169620099119", "outside": true}, {"handle": "@Xfinancebull", "sector": "CeDeFi", "claim": "Ripple Treasury (GTreasury) + Ripple Prime framed as corporate/institutional rails for holding and deploying XRP as productive capital.", "eng": "986", "time": "Aug 08 21:01", "url": "https://x.com/Xfinancebull/status/2086195998381027746", "outside": true}, {"handle": "@Ondo", "sector": "RWA", "claim": "CoinShares/Token Terminal: RWA deposits in DeFi lending+DEXs tripled YoY to $7.4B while broader DeFi deposits fell ~15%.", "eng": "714", "time": "Aug 09 17:05", "url": "https://x.com/Ondo/status/2086499046697443668", "outside": true}, {"handle": "@binance", "sector": "RWA / Dexs", "claim": "bStocks took 85% of tokenized-equity DEX volume in July as category volume jumped $2.9B \u2192 $8.8B.", "eng": "703", "time": "Aug 10 06:02", "url": "https://x.com/binance/status/2086694545236824539", "outside": false}, {"handle": "@ClutchMarkets", "sector": "Dexs / Token Locker", "claim": "Clutch pairs Robinhood Chain ve(3,3) DEX @uponrh with Stonk Launcher, locks ~4% float of $UP where 86% supply is already ve-locked.", "eng": "701", "time": "Aug 10 21:10", "url": "https://x.com/ClutchMarkets/status/2086923069075886302", "outside": true}, {"handle": "@JupiterExchange", "sector": "Yield", "claim": "Jupiter Lend v2 Smart Vaults let deposits earn lending yield and DEX swap fees on the same capital.", "eng": "699", "time": "Aug 10 14:59", "url": "https://x.com/JupiterExchange/status/2086829920378814794", "outside": false}, {"handle": "@thegaboeth", "sector": "Dexs", "claim": "DefiLlama: Robinhood Chain already posts higher 24h DEX volume than Ethereum L1.", "eng": "170", "time": "Aug 10 15:02", "url": "https://x.com/thegaboeth/status/2086830428963688550", "outside": true}, {"handle": "@ethereuminsti", "sector": "RWA", "claim": "Robinhood Chain (~6 weeks live) leads RWA holders: 420k+ holders, $1.3B TVL, 3.4M MAU on Arbitrum/Ethereum.", "eng": "158", "time": "Aug 10 15:36", "url": "https://x.com/ethereuminsti/status/2086839079497331091", "outside": true}];
const discoveryMeta = {"verified": 10, "proposed": 10, "dropped": 0, "shown": 8, "outside": 8, "followed": 67};
const narrativeDesk = [{"label": "Crypto Narrative", "accent": "spice", "bullets": ["Macro and infrastructure: Fed\u2019s late-July meeting, clustered US data, and ETF outflows frame a \u201cmacro week,\u201d while big", "traditional players deepen crypto infrastructure, making prediction markets feel like capital-markets plumbing.", "Prediction markets focus: Polymarket and Kalshi volume cluster around Fed path, inflation, and structural macro variables; Fed", "outcome markets act as live telemetry for macro narratives and sit near regulated derivatives territory."]}, {"label": "AI Discourse", "accent": "water", "bullets": ["Multi-agent coding patterns: Anthropic Labs shows a three-agent (planner, generator, evaluator) setup where full context resets", "plus structured state handoffs beat in-place summarization for long coding sessions, validating your cron + artifact approach.", "Hierarchical context governance: Repo practitioners and Osmani\u2019s spec framework converge on persistent CLAUDE.md-style files,", "but recommend hierarchical, per-directory specs treated as version-controlled artifacts with PR review to keep context precise"]}, {"label": "SaaS Gaps", "accent": "safe", "bullets": ["Discord support gap: Multiple self-hosting threads show teams unhappy using Discord for product support, citing poor ticketing,", "customer identity, and searchable history, opening room for a focused support community tool.", "Self-hosting and workflow pain: Redditors highlight frustration with complex rootless container deployment and small-business", "\"one missing workflow\" issues, suggesting demand for opinionated deployment UX and narrow, high-value automations."]}];

function chgClass(v: number) { return v >= 0 ? 'text-safe' : 'text-crimson'; }
function chgStr(v: number) { return `${v >= 0 ? '▲ +' : '▼ '}${v.toFixed(2)}%`; }
---

<Layout title="brief — Tuesday, August 11, 2026" description="12-source automated market intelligence briefing" wide>
  <div class="space-y-8">

    {/* MASTHEAD */}
    <div class="text-center animate-fade-up">
      <p class="font-mono text-xs tracking-[0.2em] text-faint uppercase">{date}</p>
      <h1 class="font-display text-4xl md:text-5xl font-normal mt-2 tracking-tight">market intelligence</h1>
      <p class="font-mono text-xs tracking-[0.15em] text-faint mt-3 uppercase">12 data sources · automated briefing</p>
    </div>

    {/* EDITION BAR */}
    <div class="flex justify-between font-mono text-xs text-faint uppercase tracking-wider border-y border-border py-3 animate-fade-up delay-1">
      <span>morning edition</span>
      <span>updated {updateTime}</span>
      <span>vol. I · no. {volNum}</span>
    </div>

    {/* TICKER */}
    <div class="grid grid-cols-3 md:grid-cols-7 gap-4 py-4 border-y-2 border-border-strong animate-fade-up delay-1">
      {ticker.map((t) => (
        <div class="text-center">
          <div class="font-mono text-xs tracking-wider text-faint uppercase">{t.sym}</div>
          <div class="font-display text-lg mt-0.5">{t.pr}</div>
          {t.chg !== null && t.chg !== undefined ? (
            <span class={`font-mono text-xs ${chgClass(t.chg)}`}>{chgStr(t.chg)}</span>
          ) : t.label ? (
            <span class={`font-mono text-xs ${parseInt(fng.value) <= 30 ? 'text-crimson' : parseInt(fng.value) >= 60 ? 'text-safe' : 'text-dim'}`}>{t.label}</span>
          ) : null}
        </div>
      ))}
    </div>

    {/* HEADLINE */}
    <div class="animate-fade-up delay-2">
      <h2 class="font-display text-3xl md:text-4xl font-normal leading-tight">{headline}</h2>
      <p class="font-mono text-sm text-dim mt-2 pb-5 border-b border-border">{subheadline}</p>
    </div>

    {/* NARRATIVE */}
    <div class="max-w-[640px] animate-fade-up delay-2">
      <p class="text-dim text-base leading-relaxed">{narrative}</p>
    </div>

    {/* NARRATIVE DESK — editorial intelligence (perplexity) */}
    {narrativeDesk.length > 0 && (
      <div class="animate-fade-up delay-2">
        <p class="font-mono text-xs text-spice uppercase tracking-[0.2em] mb-1">narrative desk</p>
        <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-5">What The Week Is Saying</h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {narrativeDesk.map((col) => (
            <div class={`border-l-2 border-${col.accent} pl-4`}>
              <h4 class={`font-mono text-xs uppercase tracking-wider mb-3 text-${col.accent}`}>{col.label}</h4>
              <ul class="space-y-2.5">
                {col.bullets.map((b) => (
                  <li class="text-dim text-sm leading-snug flex gap-2">
                    <span class={`text-${col.accent} mt-0.5`}>·</span>
                    <span>{b}</span>
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>
    )}

    {/* MAIN CONTENT */}
    <div class="grid grid-cols-1 lg:grid-cols-[2fr_1fr] gap-10 animate-fade-up delay-3">

      {/* LEFT COLUMN */}
      <div class="lg:border-r lg:border-border lg:pr-10 space-y-10">

        {/* TECHNICALS */}
        <div>
          <p class="font-mono text-xs text-spice uppercase tracking-[0.2em] mb-1">technical analysis</p>
          <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-4">Signal Dashboard</h3>
          <div class="overflow-x-auto">
            <table class="w-full font-mono text-sm">
              <thead>
                <tr class="text-faint text-xs uppercase tracking-wider border-b border-border">
                  <th class="text-left py-2 font-normal">Asset</th>
                  <th class="text-left py-2 font-normal">Signal</th>
                  <th class="text-left py-2 font-normal">Conv.</th>
                  <th class="text-right py-2 font-normal">RSI 1h</th>
                  <th class="text-right py-2 font-normal">RSI 4h</th>
                  <th class="text-right py-2 font-normal">24h</th>
                </tr>
              </thead>
              <tbody>
                {signals.map((s) => (
                  <tr class="border-b border-border">
                    <td class="py-2.5 text-text font-medium">{s.coin}</td>
                    <td class="py-2.5">
                      <span class={`text-xs uppercase tracking-wide ${s.signal === 'bullish' ? 'text-safe' : s.signal === 'bearish' ? 'text-crimson' : 'text-faint'}`}>{s.signal}</span>
                    </td>
                    <td class="py-2.5">
                      <div class="flex items-center gap-2">
                        <div class="w-16 h-1 bg-surface-2 rounded-full overflow-hidden">
                          <div class={`h-full rounded-full ${s.signal === 'bullish' ? 'bg-safe' : s.signal === 'bearish' ? 'bg-crimson' : 'bg-faint'}`} style={`width:${s.confidence}%`}></div>
                        </div>
                        <span class="text-xs text-dim">{s.confidence}</span>
                      </div>
                    </td>
                    <td class="py-2.5 text-right text-dim tabular-nums">{s.rsi1h ? Math.round(s.rsi1h) : '—'}</td>
                    <td class="py-2.5 text-right text-dim tabular-nums">{s.rsi4h ? Math.round(s.rsi4h) : '—'}</td>
                    <td class={`py-2.5 text-right tabular-nums ${s.change24h !== null ? chgClass(s.change24h) : 'text-dim'}`}>{s.change24h !== null ? chgStr(s.change24h) : '—'}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>

        {/* POSITIONING */}
        <div>
          <p class="font-mono text-xs text-spice uppercase tracking-[0.2em] mb-1">derivatives</p>
          <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-4">Futures Positioning</h3>
          <div class="space-y-6">
            {positioning.map((p) => (
              <div>
                <div class="flex justify-between items-baseline mb-2">
                  <span class="font-display text-lg">{p.coin}</span>
                  <span class="font-mono text-xs text-faint">OI: {p.oi} · {p.oiTrend}</span>
                </div>
                <div class="flex items-center gap-3 mb-1">
                  <span class="font-mono text-xs text-faint uppercase w-12">Retail</span>
                  <div class="flex-1 h-4 bg-surface-2 rounded overflow-hidden flex">
                    <div class="bg-safe/60 flex items-center justify-center text-xs font-mono text-text" style={`width:${p.retailLong}%`}>{p.retailLong.toFixed(0)}%</div>
                    <div class="bg-crimson/40 flex items-center justify-center text-xs font-mono text-text" style={`width:${100 - p.retailLong}%`}>{(100 - p.retailLong).toFixed(0)}%</div>
                  </div>
                </div>
                <div class="flex items-center gap-3">
                  <span class="font-mono text-xs text-faint uppercase w-12">Whales</span>
                  <div class="flex-1 h-4 bg-surface-2 rounded overflow-hidden flex">
                    <div class="bg-water/60 flex items-center justify-center text-xs font-mono text-text" style={`width:${p.whaleLong}%`}>{p.whaleLong.toFixed(0)}%</div>
                    <div class="bg-crimson/30 flex items-center justify-center text-xs font-mono text-text" style={`width:${100 - p.whaleLong}%`}>{(100 - p.whaleLong).toFixed(0)}%</div>
                  </div>
                </div>
                {p.divergence && (
                  <div class="mt-2 border-l-2 border-spice bg-surface-2 px-3 py-2 font-mono text-xs text-spice">
                    ⚠ {p.divergence}
                  </div>
                )}
                {p.retailTrend && p.retailTrend !== "stable" && (
                  <p class="font-mono text-xs text-crimson mt-1">↗ {p.retailTrend}</p>
                )}
              </div>
            ))}
          </div>
        </div>

        {/* MACRO */}
        <div>
          <p class="font-mono text-xs text-spice uppercase tracking-[0.2em] mb-1">federal reserve</p>
          <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-4">Macro Intelligence</h3>
          <div class="border-l-2 border-water bg-surface p-5 rounded-r-lg mb-6">
            <h4 class="font-display text-2xl text-water mb-3">{cycle.position}</h4>
            <div class="space-y-1.5 font-mono text-sm">
              <div class="flex justify-between"><span class="text-faint">Real Rate</span><span class={`${cycle.realRateDir === 'restrictive' ? 'text-crimson' : 'text-safe'} font-medium`}>{cycle.realRate} ({cycle.realRateDir})</span></div>
              <div class="flex justify-between border-t border-border pt-1.5"><span class="text-faint">Yield Curve</span><span class="text-text">{cycle.curve}</span></div>
              <div class="flex justify-between border-t border-border pt-1.5"><span class="text-faint">HY Spread</span><span class="text-text">{cycle.hy}</span></div>
              <div class="flex justify-between border-t border-border pt-1.5"><span class="text-faint">Unemployment</span><span class="text-text">{cycle.unemployment}</span></div>
            </div>
            <p class="font-mono text-xs text-faint mt-3">Dalio framework · Real rate = Fed Funds ({cycle.fedFunds}) − Breakeven ({cycle.breakeven})</p>
          </div>
          <table class="w-full font-mono text-sm">
            <thead>
              <tr class="text-faint text-xs uppercase tracking-wider border-b border-border">
                <th class="text-left py-2 font-normal">Indicator</th>
                <th class="text-right py-2 font-normal">Value</th>
                <th class="text-right py-2 font-normal">Change</th>
              </tr>
            </thead>
            <tbody>
              {fred.map((f) => (
                <tr class="border-b border-border">
                  <td class="py-2 text-dim">{f.label}</td>
                  <td class="py-2 text-right text-text tabular-nums">{f.value}</td>
                  <td class={`py-2 text-right tabular-nums ${chgClass(f.change)}`}>
                    {f.change >= 0 ? '+' : ''}{f.change.toFixed(2)}
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>

      {/* SIDEBAR */}
      <div class="space-y-8">
        <div>
          <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-4">Market Sentiment</h3>
          <div class="text-center py-4">
            <div class={`font-display text-6xl ${parseInt(fng.value) <= 25 ? 'text-crimson' : parseInt(fng.value) >= 60 ? 'text-safe' : 'text-dim'}`}>{fng.value}</div>
            <div class={`font-mono text-sm uppercase tracking-[0.2em] mt-1 ${parseInt(fng.value) <= 25 ? 'text-crimson' : parseInt(fng.value) >= 60 ? 'text-safe' : 'text-dim'}`}>{fng.label}</div>
            <div class="mt-4 h-1.5 rounded-full overflow-hidden" style="background:linear-gradient(90deg, var(--color-crimson), var(--color-spice), var(--color-safe))">
              <div class="relative w-full h-full">
                <div class="absolute top-[-3px] w-1 h-[12px] bg-text rounded-sm" style={`left:${fng.value}%;transform:translateX(-50%)`}></div>
              </div>
            </div>
            <p class="font-mono text-xs text-faint mt-2">0 = extreme fear · 100 = extreme greed</p>
          </div>
        </div>

        <div>
          <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-4">Market Overview</h3>
          <div class="space-y-0 font-mono text-sm">
            {[
              {label:"Total Market Cap", value:macro.totalMC},
              {label:"BTC Dominance", value:`${macro.btcDom.toFixed(1)}%`},
              {label:"ETH Dominance", value:`${macro.ethDom.toFixed(1)}%`},
              {label:"10Y Yield", value:`${macro.TNX.toFixed(2)}%`},
              {label:"Gold", value:`$${macro.GLD.toFixed(0)}`},
            ].map((m) => (
              <div class="flex justify-between py-2 border-b border-border">
                <span class="text-faint">{m.label}</span>
                <span class="text-text tabular-nums">{m.value}</span>
              </div>
            ))}
          </div>
        </div>

        <div>
          <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-4">Key Signals</h3>
          {positioning.filter(p => p.divergence).map((p) => (
            <div class="border-l-2 border-spice bg-surface px-3 py-2 mb-2 font-mono text-xs text-spice">
              ⚠ {p.coin}: {p.divergence}
            </div>
          ))}
          {positioning.filter(p => p.retailTrend && p.retailTrend !== "stable").map((p) => (
            <div class="border-l-2 border-crimson bg-surface px-3 py-2 mb-2 font-mono text-xs text-crimson">
              ↗ {p.coin} retail {p.retailTrend}
            </div>
          ))}
        </div>

        <div>
          <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-4">Prediction Markets</h3>
          <div class="space-y-0">
            {polymarket.map((m) => (
              <div class="py-3 border-b border-border">
                <p class="text-dim text-sm leading-snug">{m.q}</p>
                <p class="font-mono text-xs text-faint mt-1">
                  <span class={m.yes > 50 ? 'text-safe font-medium' : 'text-crimson font-medium'}>{m.yes}% Yes</span>
                  <span class="mx-2">·</span>
                  Vol: {m.vol}
                </p>
              </div>
            ))}
          </div>
          {oddMarkets.length > 0 && (
            <div class="mt-4">
              <h4 class="font-mono text-xs text-faint uppercase tracking-wider mb-2">kalshi · oddpool</h4>
              <div class="space-y-0">
                {oddMarkets.map((m) => (
                  <div class="py-2.5 border-b border-border">
                    <p class="text-dim text-sm leading-snug">{m.q}</p>
                    <p class="font-mono text-xs text-faint mt-1">
                      <span class={m.yes > 50 ? 'text-safe font-medium' : 'text-crimson font-medium'}>{m.yes}% Yes</span>
                      <span class="mx-2">·</span>
                      Vol: {m.vol}
                    </p>
                  </div>
                ))}
              </div>
            </div>
          )}
        </div>
      </div>
    </div>

    {/* EMERGING CATEGORIES */}
    <div class="animate-fade-up delay-4">
      <p class="font-mono text-xs text-spice uppercase tracking-[0.2em] mb-1">what is starting</p>
      <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-2">Emerging DeFi Categories</h3>
      <p class="font-mono text-xs text-faint mb-5">
        {emergingMeta.scanned.toLocaleString()} protocols · {emergingMeta.scored} categories scored ·
        ranked by momentum × breadth × room to grow, not by size
      </p>
      <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
        {emerging.map((c, i) => (
          <div class="border border-border rounded-xl bg-surface p-5 flex flex-col gap-3">
            <div class="flex items-baseline justify-between gap-3">
              <div class="flex items-baseline gap-2 min-w-0">
                <span class="font-mono text-xs text-faint">{String(i + 1).padStart(2, '0')}</span>
                <span class="font-display text-base truncate">{c.name}</span>
              </div>
              <span class="font-mono text-lg text-spice shrink-0">{c.score.toFixed(1)}</span>
            </div>

            <div class="flex items-baseline gap-3 font-mono text-xs">
              <span class="text-faint">{c.tvl}</span>
              <span class={chgClass(c.mom)}>{chgStr(c.mom)} 7d</span>
              {c.new90 > 0 && (
                <span class="text-water">+{c.new90} new</span>
              )}
            </div>

            {/* breadth is the honest number — one protocol mooning vs the whole category moving */}
            <div>
              <div class="flex justify-between font-mono text-[10px] text-faint uppercase tracking-wider mb-1">
                <span>breadth</span>
                <span>{c.breadth}% of {c.protocols} up</span>
              </div>
              <div class="h-1.5 rounded-full bg-surface-2 overflow-hidden">
                <div class={`h-full rounded-full ${c.breadth >= 50 ? 'bg-safe' : c.breadth >= 30 ? 'bg-spice' : 'bg-faint'}`}
                     style={`width:${Math.max(c.breadth, 2)}%`}></div>
              </div>
            </div>

            <div class="font-mono text-[10px] text-faint flex justify-between gap-2 pt-1 border-t border-border">
              <span class="truncate">led by {c.leader}</span>
              <span class="shrink-0">{c.whale}% in one</span>
            </div>
          </div>
        ))}
      </div>
      <p class="font-mono text-[10px] text-faint mt-3">
        {emergingMeta.days < 8
          ? `acceleration needs 7d of history — ${8 - emergingMeta.days} more night${8 - emergingMeta.days === 1 ? '' : 's'}`
          : 'Δ7 shows whether a category is speeding up or rolling over'}
        · centralised exchanges excluded · categories under $100M excluded
      </p>
    </div>

    {/* OUTSIDE THE BUBBLE */}
    {discovery.length > 0 && (
      <div class="animate-fade-up delay-4">
        <p class="font-mono text-xs text-spice uppercase tracking-[0.2em] mb-1">who you are not following</p>
        <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-2">Outside the Bubble</h3>
        <p class="font-mono text-xs text-faint mb-5">
          searched all of X, every post id resolved against the API ·
          <span class="text-spice">{discoveryMeta.outside} of {discoveryMeta.verified}</span> from accounts
          your {discoveryMeta.followed}-account list does not follow
          {discoveryMeta.shown < discoveryMeta.verified && ` · showing top ${discoveryMeta.shown}`}
        </p>
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-3">
          {discovery.map((p, i) => (
            <a href={p.url} target="_blank" rel="noopener noreferrer"
               class="group border border-border rounded-xl bg-surface p-4 flex flex-col gap-2
                      hover:border-border-strong hover:bg-surface-2 transition-colors">
              <div class="flex items-baseline justify-between gap-3">
                <div class="flex items-baseline gap-2 min-w-0">
                  <span class="font-mono text-xs text-faint shrink-0">{String(i + 1).padStart(2, '0')}</span>
                  {/* anchors inherit --color-accent, which paints the whole card orange.
                       Pin the ladder explicitly: handle > claim > meta. */}
                  <span class="font-display text-base truncate text-text group-hover:text-spice transition-colors">{p.handle}</span>
                  {p.outside && (
                    <span class="font-mono text-[10px] text-water uppercase tracking-wider shrink-0">new</span>
                  )}
                </div>
                <span class="font-mono text-xs text-faint shrink-0">{p.eng}</span>
              </div>
              <p class="text-sm leading-snug text-dim">{p.claim}</p>
              <div class="flex items-baseline justify-between gap-2 pt-2 border-t border-border font-mono text-[10px] text-faint">
                <span class="truncate uppercase tracking-wider">{p.sector}</span>
                <span class="shrink-0">{p.time}</span>
              </div>
            </a>
          ))}
        </div>
        <p class="font-mono text-[10px] text-faint mt-3">
          {discoveryMeta.dropped > 0
            ? `${discoveryMeta.dropped} cited post${discoveryMeta.dropped === 1 ? '' : 's'} failed to resolve and ${discoveryMeta.dropped === 1 ? 'was' : 'were'} dropped`
            : 'every cited post resolved'}
          · handle, engagement and timestamp come from the X API, never from the model
        </p>
      </div>
    )}

    {/* BOTTOM */}
    <div class="animate-fade-up delay-4">
      <p class="font-mono text-xs text-spice uppercase tracking-[0.2em] mb-1">on-chain flow</p>
      <h3 class="font-display text-xl border-b-2 border-border-strong pb-1 mb-6">DEX Activity & Trending Pools</h3>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="border border-border rounded-xl bg-surface p-5">
          <h4 class="font-mono text-xs text-faint uppercase tracking-wider mb-3">trending pools</h4>
          {trendingPools.map((p) => (
            <div class="py-2 border-b border-border last:border-b-0">
              <div class="font-display text-sm">{p.name}</div>
              <div class="font-mono text-xs text-faint">
                {p.network} · {p.vol} <span class={chgClass(p.change)}>{chgStr(p.change)}</span>
              </div>
            </div>
          ))}
        </div>
        <div class="border border-border rounded-xl bg-surface p-5 md:col-span-2">
          <h4 class="font-mono text-xs text-faint uppercase tracking-wider mb-3">defi tvl — top 8</h4>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-6">
            {tvl.map((p) => (
              <div class="flex justify-between py-2 border-b border-border">
                <span class="font-display text-sm">{p.name}</span>
                <span class="font-mono text-xs text-faint">
                  {p.tvl} <span class={chgClass(p.change)}>{p.change >= 0 ? '+' : ''}{p.change}%</span>
                </span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>

    <div class="text-center font-mono text-xs text-faint tracking-wider uppercase border-t border-border pt-6">
      {date} · data from 12 sources · not financial advice
    </div>
  </div>
</Layout>
